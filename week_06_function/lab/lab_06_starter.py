"""
Lab 06 — Function
วิธีรัน: python lab_06_starter.py
นโยบาย AI สัปดาห์นี้: ใช้ได้เฉพาะช่วยอธิบาย/debug ต้อง disclose และอธิบายทุกบรรทัดได้เอง
"""

# ─── TODO 1: function พื้นฐาน (Easy) ─────────────────────
# สิ่งที่ต้องทำ: เขียน function ชื่อ multiply(a, b) return a * b
#              เรียกใช้ multiply(6, 7) แล้วพิมพ์ผลลัพธ์
# Expected output: 42
# ─────────────────────────────────────────────────────────
raise NotImplementedError("TODO 1: implement this")


# ─── TODO 2: function คืนค่า boolean (Medium) ────────────
# สิ่งที่ต้องทำ: เขียน function ชื่อ is_even(n) — ถ้า n หารด้วย 2 ลงตัว return True
#              ไม่งั้น return False (ใช้ if ปกติ ห้าม ternary)
#              เรียกใช้ is_even(14) แล้วพิมพ์ผลลัพธ์
# Expected output: True
# ─────────────────────────────────────────────────────────
raise NotImplementedError("TODO 2: implement this")


# ─── TODO 3: Decomposition (Hard) ────────────────────────
# สิ่งที่ต้องทำ: เขียน 2 function:
#   calculate_bmi(weight, height) -> return weight / (height * height)
#   classify_bmi(bmi) -> ถ้า bmi < 18.5 return "ผอม"
#                        ถ้า bmi < 25   return "ปกติ"
#                        ไม่งั้น         return "เกินมาตรฐาน"
#              กำหนด w = 65, h = 1.7 คำนวณ bmi แล้ว
#              พิมพ์ round(bmi, 1) บรรทัดแรก พิมพ์ classify_bmi(bmi) บรรทัดสอง
# Pattern ที่ใช้: Decomposition (แตกงานเป็น 2 ฟังก์ชัน)
# Expected output: 22.5
#                  ปกติ
# ─────────────────────────────────────────────────────────
raise NotImplementedError("TODO 3: implement this")
